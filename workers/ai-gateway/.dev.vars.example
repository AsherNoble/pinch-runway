# Development-only placeholder used to generate a string binding type.
# Put the real secret in `.dev.vars` locally or with `wrangler secret put`.
WORKERS_AI_GATEWAY_TOKEN=replace-with-at-least-32-random-characters
